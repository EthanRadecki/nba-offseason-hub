// NBA_TEAMS — optional inline data store.
// You can either populate team data here directly,
// OR leave this empty and put each team's data in data/{slug}.json
// The app will check this object first, then fall back to fetching the JSON file.

const NBA_TEAMS = {
  // "new-york-knicks": { ... }  ← add teams here once you have data
};
